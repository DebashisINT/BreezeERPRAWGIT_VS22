
@model CRM.Models.EnquiriesDet

<div class="">

  
    <div class=" ">
        <div class="panel-heading" role="tab" id="headingOne">
            <h4 class="Widgheading relative clearfix" aria-expanded="true" aria-controls="collapseOne"></h4>
        </div>

        <div>
            @*<label>Select Industry</label>
            @Html.DevExpress().ComboBox(
                    settings =>
                    {
                        settings.Name = "cmbPriorIndustry";
                        settings.Properties.ClientInstanceName = "ccmbPriorIndustry";
                        settings.Properties.TextField = "ind_industry";
                        settings.Properties.ValueField = "ind_id";
                        settings.Properties.DropDownStyle = DropDownStyle.DropDown;
                        settings.Properties.ValueType = typeof(int);
                        settings.Properties.IncrementalFilteringMode = IncrementalFilteringMode.StartsWith;
                        settings.SelectedIndex = 1;
                        settings.Properties.ClientSideEvents.SelectedIndexChanged = "PriorIndustry_SelectedIndexChanged";

                        settings.PreRender = (sender, e) =>
                        {
                            if (ViewBag.EnqFrom != "")
                            {
                                ASPxComboBox com = (ASPxComboBox)sender;
                                com.Text = ViewBag.EnqFrom;
                            }

                        };

                    }).BindList(Model.listAssIndustry).GetHtml()*@
            <label>Select Salesman</label>
            @Html.DevExpress().ComboBox(
                    settings =>
                    {
                        settings.Name = "cmbPriorSalesman";
                        settings.Properties.ClientInstanceName = "ccmbPriorSalesman";
                        settings.Properties.TextField = "Salesman_Name";
                        settings.Properties.ValueField = "Salesman_Id";
                        settings.Properties.DropDownStyle = DropDownStyle.DropDown;
                        settings.Properties.ValueType = typeof(int);
                        settings.Properties.IncrementalFilteringMode = IncrementalFilteringMode.StartsWith;
                        settings.SelectedIndex = 1;
                        settings.Properties.ClientSideEvents.SelectedIndexChanged = "PriorSalesman_SelectedIndexChanged";

                        settings.PreRender = (sender, e) =>
                        {
                            if (ViewBag.EnqFrom != "")
                            {
                                ASPxComboBox com = (ASPxComboBox)sender;
                                com.Text = ViewBag.EnqFrom;
                            }

                        };

                    }).BindList(Model.listAssignedSalesman).GetHtml()
        </div>
        <div class="clear"></div>
        <hr />

        <div class="GridViewArea">
            @{
                Html.RenderAction("PartialSetPriority", "Enquiries");
            }
        </div>

        <div class="text-center mTop10">
            @*<button type="button" class="btn btn-success" onclick="SelectAll();">Select All</button>*@
            <button type="button" class="btn btn-success" onclick="OnClickBulkSetPriority();">Confirm</button>
        </div>

    </div>


</div>


